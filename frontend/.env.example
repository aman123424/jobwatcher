# Copy this file to .env.local (gitignored) to point the frontend at a
# different backend than the default (http://127.0.0.1:8000, matching
# `uvicorn api:app --reload`'s default local address - see backend/api.py).
VITE_API_BASE_URL=http://127.0.0.1:8000
